import { Client } from 'xmlrpc';
import { ClientOptions } from "./types";
export declare function getFullMethodName(baseName: string, method: string): string;
export declare function makeRpcClient(options: ClientOptions): Client;
